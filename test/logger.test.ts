import { logger } from "../src/logger";

describe('logger', () => {
    const mockConsole = jest.fn();

    beforeEach(() => {
        mockConsole.mockClear();
    });

    it('', () => {
    });
});
