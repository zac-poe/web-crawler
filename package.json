{
  "name": "web-crawler",
  "version": "1.0.0",
  "description": "simple web navigation and resource downloading",
  "main": "dist/index.js",
  "bin": {
    "web-crawler": "./cli.js"
  },
  "scripts": {
    "postinstall": "npm run build:rebuild",
    "clean": "rm -rf dist/",
    "build": "npm test && npm run build:rebuild",
    "build:rebuild": "npm run clean && npm run build:compile",
    "build:compile": "tsc -b",
    "test": "jest --silent"
  },
  "dependencies": {
    "@types/node": "^15.12.2",
    "@types/command-line-args": "^5.0.1",
    "command-line-args": "^5.2.0",
    "@types/command-line-usage": "^5.0.2",
    "command-line-usage": "^6.1.0",
    "yaml": "^1.10.2",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "typescript": "^4.3.5",
    "ts-node": "^10.1.0",
    "jest": "^27.0.6",
    "ts-jest": "^27.0.4"
  }
}
